import Image from "next/image";
import { signIn } from "./actions";

export default async function LoginPage({ searchParams }: { searchParams: Promise<{ message?: string; next?: string }> }) {
  const { message, next } = await searchParams;
  return <main className="grid min-h-screen place-items-center bg-surface p-5"><form action={signIn} className="w-full max-w-md rounded-xl border border-line bg-surface-raised p-7 shadow-2xl"><div className="flex items-center gap-3"><div className="grid size-11 place-items-center overflow-hidden rounded-lg bg-accent"><Image src="https://hnzyllbksqvamqfubhri.supabase.co/storage/v1/object/public/brand-assets/logo/3f8bbbcd-c9da-47df-ad20-f801d397610a/logo.png" alt="doscientos" width={44} height={44} /></div><div><p className="font-semibold">NEVAL 5</p><p className="text-xs text-muted">Gestión de fábrica</p></div></div><h1 className="mt-8 text-2xl font-semibold">Accede a tu espacio</h1><p className="mt-2 text-sm text-muted">Usa la invitación recibida de tu administrador.</p>{message && <p className="mt-5 rounded-lg border border-destructive/40 bg-destructive/10 px-3 py-2 text-sm text-red-200">{message}</p>}<input type="hidden" name="next" value={next || "/"} /><label className="mt-6 block text-sm font-medium">Email<input required name="email" type="email" autoComplete="email" className="mt-1.5 w-full rounded-lg border border-line bg-surface px-3 py-2.5 outline-none focus:border-accent" placeholder="tu@empresa.com" /></label><label className="mt-4 block text-sm font-medium">Contraseña<input required name="password" type="password" autoComplete="current-password" className="mt-1.5 w-full rounded-lg border border-line bg-surface px-3 py-2.5 outline-none focus:border-accent" /></label><button className="mt-7 w-full rounded-lg bg-accent py-3 text-sm font-bold text-accent-foreground hover:bg-accent/90">Entrar</button></form></main>;
}
